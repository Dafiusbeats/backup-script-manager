# backup-script-manager
- When running the script, it searches for every .txt file in the current folder.
- Copies each one to the 'backup' folder, adding '_backup' to the name.
- Deletes the original files.
- All actions are logged to a file log.txt .
